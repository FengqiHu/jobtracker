{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && pnpm install && pnpm build && cd ../backend && pnpm install && pnpm build"
  },
  "deploy": {
    "startCommand": "cd backend && pnpm db:migrate && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
